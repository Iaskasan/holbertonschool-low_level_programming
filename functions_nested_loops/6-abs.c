#include "main.h"

/**
* _abs - returns 1 if character is lowercase else 0
* @x: The character to be checked.
* Return: Always 0 (Success)
*/

int _abs(int x)
{
{
	if (x < 0)
{
	return (-x);
}
	else
{
	return (x);
}
}
	return (0);
}
