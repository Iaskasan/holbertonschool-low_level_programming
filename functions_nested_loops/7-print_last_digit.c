#include "main.h"
/**
* print_last_digit - returns 1 if character is lowercase else 0
* @x: The character to be checked.
* Return: Always 0 (Success)
*/

int print_last_digit(int x)
{
	x = x % 10;
{
	_putchar(x);
}
	return (0);
}
